import ProductDetailPage from "./ProductDetailPage";

// Preview harness only — the deliverable is ProductDetailPage.tsx itself,
// which carries its own mock data and renders standalone.
export default function App() {
  return <ProductDetailPage />;
}
